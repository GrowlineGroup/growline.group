// ─── Fallback-Daten (Events) ─────────────────────────────────────────────────

import type { Event, KalenderEintrag, EventText } from './types';

export const FALLBACK_EVENTS: Event[] = [];
export const FALLBACK_KALENDER: KalenderEintrag[] = [];
export const FALLBACK_EVENT_TEXTE: EventText[] = [];
